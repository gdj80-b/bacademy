<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goodee.bacademy.mapper.StudentWishListMapper">
	
	<!-- 내가 찜한 강의 리스트 출력  -->
	<select id="getMyWishList" resultType="com.goodee.bacademy.vo.WishList">
		SELECT  b.lecture_no, b.lecture_name, b.lecture_price, b.room_num, b.lecture_time, b.lecture_day, b.start_date, b.end_date
		FROM wishlist a
			JOIN lecture b ON a.lecture_no = b.lecture_no
		where a.id = 'student'
		ORDER BY a.create_date;
	</select>
</mapper>

